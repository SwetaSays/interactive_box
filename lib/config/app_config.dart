class AppConfig {
  static const int minBoxes = 5;
  static const int maxBoxes = 25;
  static const double boxSpacing = 8.0;
  static const double boxBorderRadius = 8.0;
  static const Duration animationDuration = Duration(milliseconds: 300);
  static const Duration reverseDelay = Duration(seconds: 1);
}
